import { ShowBlockDirective } from './show-block.directive';

describe('ShowBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowBlockDirective();
    expect(directive).toBeTruthy();
  });
});
