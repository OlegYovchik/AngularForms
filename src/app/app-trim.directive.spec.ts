import { AppTrimDirective } from './app-trim.directive';

describe('AppTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTrimDirective();
    expect(directive).toBeTruthy();
  });
});
